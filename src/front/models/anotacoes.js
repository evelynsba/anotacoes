
class Anotacoes{
    constructor(titulo, notas){
        titulo = this.titulo;
        notas = this.notas;
    
    try {
        if(this.titulo == "" && this.notas == ""){

        }
    } catch (error) {
        throw new error("Criacao de notas - dados invalidos")
    }
}
}